# Issues

- [ ] Add multi binary install if the archive or the build support/produces multiple bin
  - [ ] This I think would convert the `binary` plugin schemas to `binaries` = [], do what's best to implement this
